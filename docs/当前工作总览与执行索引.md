# Fangrush 当前工作总览

> 文档总结构见 [文档中心](README.md)。本文只回答当前目标、执行顺序和工作入口；任务状态以 [任务清单](任务/任务清单.md) 为准。

## 当前目标

当前先聚焦一批用户体验收口：复核两个正式域名，处理用户已记录的 Admin、关卡流程、棋盘交互、帮助指导、奖励、加载和分享问题，并主动检查同类路径。随后进行24关逐步体验验证和 H5 提交材料准备。玩家端、Admin 和最终代码架构拆分已经完成；平台上架和真实 SDK 不作为当前硬阻塞。

## 当前待办事实

- `apps/player-web` 与 `apps/admin` 已完成独立应用拆分；旧 `apps/web` 已退役，共享资源由 `packages/web-assets` 维护。
- 部署边界已确定：同一 monorepo 建两个 Vercel Project，玩家端使用 `apps/player-web`，Admin 使用 `apps/admin`；本地分别是 `5003`、`5002`。
- Admin 路由已统一：正式首页为 `/admin`，登录页为 `/admin/gate`，功能页全部位于 `/admin/*`；旧根级 `/skins`、`/levels`、`/ai`、`/gate` 不再作为 Admin 路由。
- `fangrush.com` 玩家端和 `admin.fangrush.com` Admin 均已部署成功；后续只记录线上回归问题，不再把部署作为当前待办。
- 24关玩家最终确认仍为0/24，等当前体验问题收口后，用户可按时间逐步验证，不要求一次完成。
- 当前反馈唯一入口为 [用户体验提升0718](文档整理todo/用户体验提升0718.md)；新问题先追加到该记录，再按规则、地图/开局、AI、UI/教学、奖励、平台或技术异常归因。
- 长期产品判断参考见 [产品体验与商业化原则](产品核心/产品体验与商业化原则.md)；0718 专项结论见 [0718 用户体验与商业化专项](产品核心/0718用户体验与商业化专项.md)。
- 自动化测试、CSS 产物检查、公开路由检查和发布门禁可继续复用；线上问题以正式域名复现证据为准。
- 第一至五批体验代码修复已完成：通关后的 Continue 进入下一关、Next hunt 提升为主操作、结果层移动端固定可见、分享增加纯文本/复制/下载降级；棋盘只把狼和合法落点作为可操作状态，岩石补充地形语义并移除正常资源加载时的错误圆形占位；帮助页增加明确返回入口，局内帮助补充操作状态说明，Admin AI 增加用途、指标和人工验收边界说明；皮肤页展示四季碎片余额，奖励无掉落不再显示 `+0`，广告 Mock 覆盖失败和重复生命周期；Admin 首页、经济页和检查清单补充工作顺序、数据边界和移动端表格说明；仍待正式域名和真实设备确认。
- 2026-07-18 已在停止残留开发进程后完成干净发布验证：`pnpm release:check` 的 14 个门禁全部 PASS，玩家端与 Admin 生产构建均成功；`pnpm check:runtime-routes` 覆盖玩家端和 Admin 共 56 条路由并全部返回 200。Admin 正式入口统一为 `/admin/*`，旧根级功能路由不再生成。
- 最新公开入口检查：`pnpm check:runtime-routes` 覆盖首页、章节、春季入口和全部 24 个 `/play/<levelId>` 路由，均返回 200；资源、皮肤、平台边界和应用源码边界检查同时通过。
- Poki/CrazyGames真实SDK、App、原生广告、IAP、账号和云存档不进入当前执行主线。

## 执行顺序

1. `T-016`：先处理已记录的体验问题，并主动检查相关同类路径。
2. `T-019`：完成两个正式域名的线上回归记录，处理可复现部署问题。
3. `T-005`：体验收口后，用户按时间逐步试玩24关，我根据反馈做归因、修复、复测和关卡文档更新。
4. `T-020` / `T-021`：补齐关卡资料，并根据用户反馈收口 UI、无障碍和奖励表达。
5. `T-022`：继续准备 Poki/CrazyGames 材料；只有官方条件明确后才启动 `T-017` 的真实 SDK。
6. `T-023`：持续维护 Vercel 发布操作；旧 `apps/web` 退役已完成，不再作为当前任务。

## 工作入口

- [当前任务](任务/任务清单.md)
- [暂缓需求](任务/需求池.md)
- [核心产品定义](产品核心/01-核心产品定义.md)
- [24关状态](产品核心/24关/README.md)
- [渠道发展路线](商业成功/渠道发展路线.md)
- [发行资料](../distribution/README.md)
- [未来游戏内容](产品扩展/README.md)
- Admin：`/admin/levels`、`/admin/ai`、`/admin/checklist`
