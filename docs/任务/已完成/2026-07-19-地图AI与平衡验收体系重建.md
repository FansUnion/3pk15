# 地图、AI 与平衡验收体系重建完成记录

## 结果

T-025 已完成。新增策略性连吃、拓扑分析、羊方优势指标、有限求解、统一报告、风险归因和 24 关复验。

最终统一口径：固定 5 个种子，羊方使用关卡线上档位，Hard AI 使用线上默认 `maxNodes=80`，狼方比较 `random / mixed / chain-aware`。

最终自动基线：

- `pass`：3 关；
- `review`：18 关；
- `reject`：3 关；
- 人工验收：`0/24`；
- 有限求解：没有初始局面获得单方必胜证明。

## 校正记录

第一版矩阵曾误用 Hard AI `maxNodes=40`。发现后撤回完成状态，统一改为 `80` 并按四季重新运行；最终分类和各关主要结果未变化。详细证据见[第四批地图、AI与求解诊断](../../产品核心/验证记录/2026-07-18-第四批地图AI与求解诊断.md)。

## 验证

- 四季 24 关统一矩阵拆分运行全部通过；
- `winter-02` 三组候选实验拆分运行全部通过；
- `pnpm --filter @wolf-sheep/game-core lint` 通过；
- 完整回归最终为 `16` 个测试文件、`77` 个测试通过；可选长任务按环境变量跳过。

## 后续

T-005 后续已完成。三批共重建 15 张核心地图，其余 9 张通过职责和新鲜度审计后保留。最终生产结果：

- 生产自动验收：`24/24`；
- 诊断标签：`8 pass / 16 review / 0 reject`；
- 16 个 `review` 只包含策略敏感或连吃策略敏感，不含长尾、死角、同构、首次捕食阻塞、机械强胜或异常终局；
- 非空岩石布局和完整开局在镜像、旋转归一后均无重复；
- 每关 `chain-aware` 狼策略在固定种子中至少存在一局胜局；
- 玩家端与 Admin 共同读取 `@wolf-sheep/game-core` 的唯一关卡表，无第二份生产配置；
- 玩家端和 Admin 测试、生产构建通过，发布产物分别生成全部24关路由；
- 24 个玩家试玩页完成本地运行时检查：HTTP 200、关卡 ID、棋盘 SVG、唯一浏览器标题和无服务端错误全部通过；
- 24 个玩家试玩页的发布载荷均包含 3 狼、15 羊和当前岩石配置，未携带旧地图布局；
- 单关设计卡、Admin产品元数据、季节说明和英文教学点已与最终地图结构统一，无旧五石秋关或空盘冬季说明残留；
- 玩家试玩继续作为反馈与优化来源，不再阻塞生产验收。

当前证据见[24关学习曲线重建基线](../../产品核心/验证记录/2026-07-19-24关学习曲线重建基线.md)。
