# T-002 AI 行为定型

> 完成日期：2026-07-16。状态：已完成。

## 完成内容

- 在 `ai-behavior.test.ts` 固化 normal/hard 的逃跑、抱团、阻挡和避死角局面；所有档位仍验证只返回当前合法行动。
- Hard 明确为有限预测：评估一着羊行动、最坏的完整狼回合（含合法连吃）和一着后续羊回应；节点或时间预算耗尽时回退到 normal，并暴露 `degraded` 与 `lookaheadCompleted` 元数据。
- 将已有的三次完整局面重复判和从 web 会话层下沉到 `game-core`，使正式对局、AI 搜索、Admin 和平衡模拟使用同一规则；重复历史随局面序列化，旧序列化数据可兼容读取。
- 搜索模拟通过 `applyAction` 产生独立状态，回归测试确认不会改写输入棋盘。

## 验收证据

- `pnpm --filter @wolf-sheep/game-core lint`：通过。
- `pnpm --filter @wolf-sheep/game-core build`：通过。
- `pnpm --filter @wolf-sheep/game-core test`：41 项通过；8 项深度平衡用例按默认配置跳过。
- `pnpm --filter @wolf-sheep/web build`：通过。
- 关键代码：`packages/game-core/src/ai/hard.ts`、`packages/game-core/src/rules.ts`、`packages/game-core/src/serialize.ts`、`packages/game-core/tests/ai-behavior.test.ts`、`packages/game-core/tests/rules.test.ts`。

## 后续风险

- 本任务证明规则与构造性行为，不提供 24 关的胜率或和棋率结论。下一任务为 `T-003 24 关平衡矩阵`。
- 深度平衡用例默认跳过；在 T-003 使用固定预算、种子与狼策略运行并记录完整基线。
