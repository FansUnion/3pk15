# T-001 开局链路统一

> 完成日期：2026-07-16。状态：已完成。

## 完成内容

- 在 `game-core` 增加 `createLevelInitialState(level)`，统一传入关卡的 `opening`、岩石、目标吃羊数和回合上限。
- 正式对局沿用已存在的完整参数传递；AI 台、批量模拟、关卡预览、AI 行为测试和两类平衡模拟改用共享入口。
- `validateLevel` 增加开局至少存在一个狼方合法行动的校验。
- 增加全关卡开局回归测试，确保配置过非默认狼位的关卡不会回退到默认位置。

## 验收证据

- `pnpm --filter @wolf-sheep/game-core test`：35 通过，8 个深度平衡测试按默认配置跳过。
- `pnpm --filter @wolf-sheep/game-core build`：通过。
- `pnpm --filter @wolf-sheep/web build`：通过。
- 静态搜索确认所有关卡级初始化入口使用 `createLevelInitialState`；正式对局仍完整传递关卡参数。

## 后续风险

此任务只保证所有入口使用同一初始局面，不证明 AI 行为或 24 关平衡已完成。下一任务是 `T-002 AI 行为定型`。

