# T-011 / T-013 / T-014 发布基础闭环

完成日期：2026-07-17

## 完成范围

- `T-011`：任务名、皮肤名和 24 关“本关学习”具备中英文内容；皮肤、任务、设置页壳文案统一进入 `messages.ts`。
- `T-013`：默认、四季和月夜皮肤/棋盘资源通过完整性检查；预览、装备和多视口展示无资源错误。
- `T-014`：任务奖励保持幂等；通用碎片可兑换狼群套装，冬季碎片可兑换月夜棋盘；重复兑换不会再次扣除。

## 代码证据

- `packages/game-core/src/content/levels.ts`：`levelTeachingPoint()` 和 24 条英文教学点。
- `packages/game-core/src/content/quests.ts`：任务双语字段和 `questDisplayTitle()`。
- `packages/game-core/src/content/skins.ts`：皮肤双语字段、`skinDisplayName()` 和两类碎片兑换。
- `apps/web/src/i18n/messages.ts`：任务、皮肤、设置的成对壳文案。
- `apps/web/src/app/hunt/[levelId]/page.tsx`、`quests/page.tsx`、`skins/page.tsx`：按当前语言读取内容并显示奖励/成本。

## 验证证据

- `pnpm --filter @wolf-sheep/game-core test`：10 个测试文件通过，57 项通过、28 项按设计跳过。
- `pnpm --filter @wolf-sheep/game-core check:skins`：通过。
- `pnpm --filter @wolf-sheep/web build`：成功生成 71 个页面。
- 浏览器：EN/ZH 的 `/skins`、`/quests`、`/settings` 和代表性 Hunt 页面通过；英文页无中文内容字符。
- 浏览器：皮肤预览图片无加载失败；390px 和 1440px 无横向溢出。
- 浏览器：任务领取后显示已领取结果；月夜棋盘显示 30 冬季碎片成本和兑换按钮。

## 剩余边界

专业美术与音频扩充不属于本批。皮肤辨识度、奖励吸引力和文案理解度仍由最终玩家验收反馈决定，但不再阻塞这三个工程任务关闭。
