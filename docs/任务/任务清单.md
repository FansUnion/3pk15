# 任务清单

> 更新：2026-07-16 · 数字越小越先做

---

## 1 · 对局体验

| 优先 | ID | 端 | 开发时大概干啥 | 做成什么样算完 | 状态 |
|------|-----|-----|----------------|----------------|------|
| 1 | P-A03 | 两端 | 改 `BoardSvg`（或对局棋盘组件）的绘制顺序 / z 层：岩石、棋子、格线谁盖谁。一般**不碰** `game-core` 规则。 | 任意关卡：岩盖子、子盖线，无错层穿帮 | 待办 |
| 2 | P-CHAIN-UX | 两端 | 规则侧连吃已有；主要改 `PlayScreen` 交互：可连吃时「结束连吃 / 只走一格」是否好懂；要不要「默认自动连吃」及默认值；多条连吃分支怎么点选。定稿后回写创意 [`08`](../游戏创意/08-对局信息与新手引导.md) 一句。 | 玩家不靠说明书也能完成/结束连吃；行为与文档一致 | 待办 |
| 3 | P-UNDO | 两端 | 在对局会话（如 `play-store`）保留可回退的局面栈；UI 加悔棋；注意与羊 AI 回合、连吃的边界（悔到哪一步要想清）。可能要动 core 或仅壳层快照。 | 对局中可撤销上一步（或上一个完整回合，定稿时写清） | 待办 |
| 4 | P-STATS | 两端 | 对局中累计步数、用时等；结算弹层或对局 HUD 展示。多为壳层状态，不必改胜负规则。 | 玩家能看到本局步数/时长（至少结算可见） | 待办 |
| 5 | P-SHARE | 两端 | 把当前棋盘渲成图（canvas/SVG 导出）或调系统分享；注意皮肤资源与隐私。 | 能导出或分享一张本局画面 | 待办 |

---

## 2 · AI（校准 + 玩家设置）

| 优先 | ID | 端 | 开发时大概干啥 | 做成什么样算完 | 状态 |
|------|-----|-----|----------------|----------------|------|
| 6 | P-AI-FIX | 独立站 | 在 `/admin` AI 台用固定局面测 hard；改 `game-core` 的 hard 评估/搜索；补自动化测试用例（合围、该挡就挡等）。批量胜率可辅助，不是唯一标准。见 [`技术设计/03`](../技术设计/03-AI引擎.md)。 | 固定测试局面（合围、该挡的挡等）自动化测试都过；冬天关卡里羊会明显围堵狼，玩起来不像乱走 | 待办 |
| 7 | P-AI-SLIDER | 两端 | 设置页加「本局 AI 难度」；对局传入 `pickSheepAction` 的 difficulty。**不改**章节默认档绑定（春 easy 等仍是默认）。 | 玩家可临时调难/易；未调时仍跟章节默认 | 待办 |
| 8 | P-AI-PACING | 两端 | 改羊走棋前的延时（现约统一 600ms）：例如春/easy 更长、冬/hard 略短。多半只改 `PlayScreen` / play-store 定时，不改 AI 着法本身。 | 不同章「思考感」有区分，且不拖垮节奏 | 待办 |

---

## 3 · 进度与收集

| 优先 | ID | 端 | 开发时大概干啥 | 做成什么样算完 | 状态 |
|------|-----|-----|----------------|----------------|------|
| 9 | UX-S2-05 | 两端 | 改任务页（`/quests`）或主导航入口：进度条、「还差 N」、今日/本周完成态更靠前，少翻层级。逻辑已有，偏 UI 信息架构。 | 两三步内能看清任务差多少 | 待办 |
| 10 | P-D02 | 两端 | 改皮肤店/设置/结算等展示四季碎片与通用碎片的数量；季节碎片兑棋盘时数字可读。存档字段已有，偏展示。 | 四季碎片数量不用猜、兑棋盘时看得懂花了多少 | 待办 |
| 11 | P-DAILY | 两端 | **新产品能力**：定每日挑战规则或通关星级；`game-core` 内容/存档字段 + 每日刷新 + UI 入口与奖励。比上面两项重。 | 玩家有每日目标或星级可刷，能拉动回访 | 待办 |
