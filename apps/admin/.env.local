# Local admin defaults. Replace ADMIN_ACCESS_KEY only for private local testing.
# Keep the explicit shell marker so Admin APIs cannot be mistaken for a portal build.
NEXT_PUBLIC_APP_SHELL=admin
ADMIN_ENABLED=true
ADMIN_ACCESS_KEY=
